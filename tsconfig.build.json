{
  "extends": "./tsconfig.json",
  "exclude": [
    "src/test-utils",       // Utility files for testing
    "**/*.spec.ts",          // Exclude test files
    "**/*.test.ts",
    "**/*.spec.tsx",
    "**/*.test.tsx"
  ]
}